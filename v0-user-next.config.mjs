/**
 * User configuration for Next.js
 * This extends the main next.config.mjs file
 */

/** @type {import('next').NextConfig} */
const userConfig = {
  // Any custom configurations can go here
  // Explicitly using App Router only
};

export default userConfig; 